import React, { useState } from "react";
import "./Toolbar.css";

export const Toolbar = () => {

    let toolbar =
        <div className={"toolbar"}>
            Toolbar
        </div>;

    return(toolbar)
}
